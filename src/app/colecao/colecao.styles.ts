'use client';

import { flex } from "@/styles/mixins";
import styled from "styled-components";


export const SWrapper = styled.main`
    ${flex({})}
    border: solid #ff0000;
    margin-top: 10dvh;
    height: 80dvh;
`;